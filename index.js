/*
function parseTitle(post) {
  if (validation.isString(post.title)) {
    post.title = post.title.replace(/^\s+|\s+$/g, '');
    post.title = post.title.toLowerCase();
    post.title = _.trim(post.title);
    post.title = _.capitalize(post.title);
  }

  return post;
}
*/